name: Pylint

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyrefly
    - name: Analysing the code with pyrefly
      run: |
        pyrefly check
